# No secrets needed yet for this environment.
# When you have real secrets to store here (registry password, app secrets, etc.),
# add them as plain YAML below, then encrypt the file in place with:
#   ansible-vault encrypt group_vars/stage/vault.yml
# Jenkins would then need a matching --vault-password-file credential to decrypt it at deploy time.
